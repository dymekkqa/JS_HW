export enum beerValue {
    pinta = 400,
    point = 500,
    liter = 1000
}

export enum beerBrand {
    alivaryja = 0.3,
    lidskaje = 0.4,
    krynica = 0.2
}

export enum burgersTypeOfMeet {
    cheaken = 50,
    beef = 60,
    pork = 55,
    vege = 50
}

export enum cheeseTypeOfBurgers {
    cheddar = 20,
    feta = 25,
    gouda = 30
}

export enum Sauce {
    Ketchup = 1,
    Mustard = 1,
    Mayo = 2
}
